# Лог экспериментов

Формат каждой записи:
```
## [дата] [участник] — [название]
- Модель/подход: ...
- Параметры: ...
- Val RMSE (OOF, log): IC50=..., CC50=..., SI=..., avg=...
- Val RMSE (original space): IC50=..., CC50=..., SI=..., avg=...
- LB RMSE (Kaggle): ...
- Примечание: ...
```

---

## 2026-05-24 — EDA: ключевые выводы (notebooks/01_eda.ipynb)

- Таргеты логнормально распределены, нужна log-трансформация
- SI = CC50/IC50 точно (100% совпадение), следствие: log(SI) = log(CC50) - log(IC50)
- 18 константных признаков → удаляем
- Пропуски только в BCUT2D_* и зарядовых признаках → заполняем медианой
- Топ признаки по корреляции с log(IC50): MolWt, Chi0*, SMR_VSA* серии
- Мультиколлинеарность: при r>0.95 удаляется ~35 признаков

---

## 2026-05-24 — Ансамбль LGB+XGB+CatBoost с Optuna (submissions/baseline_v1.csv)

- Модель: LightGBM + XGBoost + CatBoost, взвешенный ансамбль (1/RMSE)
- Препроцессинг:
  - Удалено 18 константных признаков
  - Заполнение пропусков медианой (fit только на train)
  - log-дополнение 11 положительных дескрипторов
  - Удалено ~35 коррелированных признаков (r>0.95) → итого ~167 признаков
- Таргеты: log(IC50), log(CC50), log(SI)
- SI: прямое предсказание (со стекингом OOF IC50/CC50) + формула CC50/IC50, бленд по OOF
- Валидация: 5-fold KFold, SEED=42
- Optuna: LGB 50 trials + XGB 40 trials (3-fold CV для скорости)
- Val OOF RMSE (log): IC50=1.5752, CC50=1.2595, SI=1.4441
- Val OOF RMSE (original space): IC50=352.3, CC50=505.1, SI=782.3, avg=546.6
- LB RMSE (Kaggle): —
- Медианы предсказаний: IC50≈46.9 (train≈44 ✓), SI≈4.9 (train≈4 ✓)
- Примечание: Без bias correction (exp(σ²/2) раздувает предсказания в 3-4x)
